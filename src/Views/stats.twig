{% extends 'partials/base.twig' %}

{% block content %}
    <div style="margin-top:20px;color:rgb(0,0,0);">
        <div class="container">
            <div class="row">
                {% for stat in stats %}
                    {% include 'partials/stats/stats-card.twig' with player %}
                {% endfor %}
            </div>

            {% if pagination is defined %}
                {% include 'partials/pagination.twig' with pagination %}
            {% endif %}
        </div>
    </div>
{% endblock %}